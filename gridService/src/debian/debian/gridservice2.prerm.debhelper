# Automatically added by dh_installinit
if [ -x "/etc/init.d/gridservices2" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d gridservices2 stop || exit 0
	else
		/etc/init.d/gridservices2 stop || exit 0
	fi
fi
# End automatically added section
