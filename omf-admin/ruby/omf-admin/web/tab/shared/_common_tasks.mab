#
# List taks which are relevant in all views
#

li do
  link_to 'Goto Dashboard', {:action => 'show', :controller => :dashboard}
end
li do
  link_to 'Browse Publication Listing', {:action => :browse, :controller => :publication}
end
li do
  link_to 'Add new publication', {:controller => 'publication', :action => 'new'} #, :class => 'admin'
end
li do
  link_to 'Import from BibTeX', {:controller => 'publication', :action => 'import_bibtex'} #, :class => 'publication'
end
