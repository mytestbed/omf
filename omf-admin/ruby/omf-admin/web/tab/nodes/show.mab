div.page_header do
	div.page_header_links do
	  a 'Add', :href => 'add', :class => 'admin'
	  text " | "
    a 'Refresh', :href => '', :class => 'admin'
	end
	h1 "Configure Nodes"
end

div.page_nav do
end

div.page do
end