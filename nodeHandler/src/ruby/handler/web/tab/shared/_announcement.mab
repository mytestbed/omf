# Display an anouncement
#
if false
  div.announcement.announcement! do
    h1 do 
      span do
        link_to_function "Hide", 'Element.hide($("announcement"));', :class => 'admin'
      end
      text "Scheduled Maintenance: 9pm CST on Saturday, June 2"
    end
    p "We will be unavailable for about 30 minutes"
  end
end
